import { ConfirmDirectiveDirective } from './confirm-directive.directive';

describe('ConfirmDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
